import { google } from 'googleapis';

const SHEET_ID = process.env.GOOGLE_SHEET_ID!;
const auth = new google.auth.GoogleAuth({
  credentials: JSON.parse(process.env.GOOGLE_SERVICE_ACCOUNT_JSON!),
  scopes: ['https://www.googleapis.com/auth/spreadsheets'],
});
const sheets = google.sheets({ version: 'v4', auth });

const REAL_TEXT = `<b>🔔 Яндекс нашёл способ заработать на ваших данных. Почему это плохая новость для вашего бюджета?</b>

Яндекс Директ запустил автоматический инструмент, который сам выбирает самые прибыльные объявления для рекламодателей. По словам платформы, клиенты заработали благодаря этому <b>1,5 млрд рублей</b>. Звучит привлекательно, но за этой цифрой стоит старая схема.

📊 Что изменилось в Яндекс Директе

• Алгоритм сам решает, какие ключи и ставки вам выгодны, без вашего участия в выборе ⚠️
• Платформа оптимизирует под себя: максимизирует клики и траты, а не вашу чистую прибыль 💸
• Вы теряете контроль над стратегией и становитесь заложником обновлений алгоритма ↗️

Рекламодатели радуются краткосрочному результату, но платят за это долгосрочной зависимостью. Яндекс берёт комиссию с каждого клика, поэтому его алгоритм оптимизирует под объём трат, а не под вашу маржу.

<blockquote>Алгоритм платформы максимизирует клики и траты, а не вашу чистую прибыль</blockquote>

ℹ️ В Telegram вы работаете со своей базой клиентов напрямую, без посредников и их алгоритмов. Вы видите каждый контакт, контролируете сообщение и платите только за результат, который сами определили. Нет чёрного ящика, нет комиссии за клик, который не привёл к продаже. Данные остаются вашими.

Хотите узнать, как перевести часть бюджета из Директа в Telegram? Напишите менеджеру <b>@LeadGet_info</b> 📱.

Источник: https://www.cossa.ru/news/350430/`;

async function main() {
  const row = 10;
  await sheets.spreadsheets.values.update({
    spreadsheetId: SHEET_ID,
    range: `ContentPlan!G${row}:H${row}`,
    valueInputOption: 'RAW',
    requestBody: { values: [[REAL_TEXT, 'https://t.me/LeadGet_reviews/76']] },
  });
  console.log(`row ${row}: post/postUrl исправлены на реальное сообщение id=76`);
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
